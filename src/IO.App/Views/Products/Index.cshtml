@model IEnumerable<IO.App.ViewModels.ProductViewModel>

@{
    ViewData["Title"] = "List Products";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a class="btn btn-info" asp-action="Create">New Product</a>
</p>

<partial name="_ListProducts" />